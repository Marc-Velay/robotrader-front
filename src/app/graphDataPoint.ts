//Basic info for each data point we use in the graphs.
export class GraphDataPoint {
  id: number;
  name: string;
  source: string;
  timestamp: string;
  opening: string;
  high: string;
  low: string;
  closing: string;
}
